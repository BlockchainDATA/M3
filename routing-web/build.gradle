apply plugin: "eclipse"
apply plugin: "eclipse-wtp"
apply plugin: "war"

dependencies {
    compile project(":routing-api")
    compile group: 'com.google.code.gson', name: 'gson', version: '2.8.5'
    compile "javax.servlet:servlet-api:2.5"
}

